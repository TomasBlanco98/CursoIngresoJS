function mostrar()
{
	let numeroIngresado;
	let i;

	numeroIngresado = parseInt(prompt("Ingrese el número"));
	for(i = 0; numeroIngresado != 9; i++)
	{
		numeroIngresado = parseInt(prompt("Ingrese el número"));
	}
}//FIN DE LA FUNCIÓN